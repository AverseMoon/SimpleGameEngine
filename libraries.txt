numpy, pip install numpy # numpy is fast math
sdl2, pip install pysdl2 # sdl2 is the window manager
cython, pip install cython # cython is to speed things up about 3x
keyboard, pip install keyboard # keyboard is the keyboard